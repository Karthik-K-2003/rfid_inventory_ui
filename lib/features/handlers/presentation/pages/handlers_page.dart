import 'package:flutter/material.dart';

class HandlersPage extends StatelessWidget {
  const HandlersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Handler List", style: TextStyle(fontSize: 32)),
    );
  }
}
